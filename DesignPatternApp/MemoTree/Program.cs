namespace MemoTree
{
    class MainClass
    {
        public static void Main(string[] args)
        {
            // 初期状態を設定（キー入力モード）
            var context = new Context();
            // 処理を実行
            context.ExecuteMemoTree();
        }
    }
}
